const connection = {
    database: 'sequelize_db',
    user: 'root',
    password: '',
    host: 'localhost',
    dialect: 'mysql',
    define: {
      timestamps: false
    }
};

export default connection;