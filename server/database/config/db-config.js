const connection = {
    database: 'db_modisch',
    user: 'root',
    password: '',
    host: 'localhost',
    dialect: 'mysql',
    define: {
      timestamps: false
    }
};

export default connection;